import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  tabBarIconStyle: {
    display: 'none',
  },
  tabBarLabelStyle: {
    fontSize: 18,
    paddingBottom: 10,
  },
});

export default styles;
